import React from 'react';

const ItemInfoFaqsButton = (props) =>(
  // button triggers expansion/visibility of selector for country, field for zip
  <button className='item-info-faqs-button'>{props.expanderTitle}</button> 
)

export default ItemInfoFaqsButton;